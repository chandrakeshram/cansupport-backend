# ====================================================================================================
# application.properties
# Default properties for local development.
# These will be overridden by profile-specific files or environment variables.
# ====================================================================================================

# Spring Application Name
spring.application.name=cansupport-backend

# Logging Configuration
logging.level.org.springframework.security=DEBUG
logging.level.org.hibernate.SQL=DEBUG        

# ======== Database Connection (for Local Development - connecting to Render's External URL) ========
# Use the EXTERNAL Database URL provided by Render here.
# Replace with your actual Render external URL, username, and password.
# The ?sslmode=require is CRUCIAL for connecting to Render's hosted PostgreSQL.
spring.datasource.url=jdbc:postgresql://dpg-d25173ndiees739pui70-a.oregon-postgres.render.com/cansupport_db_pzib?sslmode=require
spring.datasource.username=chandrakesh_ram
spring.datasource.password=mO9GPT2oYZOnh8vYnrWakYsPAEblPYLd
spring.datasource.driver-class-name=org.postgresql.Driver

# ======== JPA (Hibernate) Configuration ========
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.defer-datasource-initialization=true

# ======== Mail Configuration (Mailtrap for testing) ========
spring.mail.host=sandbox.smtp.mailtrap.io
spring.mail.port=2525
spring.mail.username=4297b546c8aac6
spring.mail.password=889d2c2d862f12
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# No active profile set here by default.
# The `render` profile will be activated during deployment.